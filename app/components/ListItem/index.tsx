import React from 'react';

type Props = {
  total: number;
  fulfilled: number;
  reward: number;
};

const ListItem = (props: Props) => {
  const {} = props;
  return (
    <div className="card  h-[2rem] mb-[1rem]">
      <div>233</div>
    </div>
  );
};

export default ListItem;
